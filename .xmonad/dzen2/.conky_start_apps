conky.config = {
	background = true,
	use_xft = true,
    -- xftfont Bitstream Vera Sans:size=8
	text_buffer_size = 1048,
	out_to_console = true,
	out_to_x = false,
    -- Update interval in seconds
	update_interval = 1,
	update_interval_on_battery = 3,
	total_run_times = 0,
	double_buffer = true,
}

conky.text = [[
^fg(\#5faf5f)${exec /home/jacob/.xmonad/dzen2/start_apps}
]]
